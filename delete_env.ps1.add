###################################################################
# Delete the k8s cluster
. "$($rootPath)/kubernetes/delete.ps1" -ConfigPath "$ConfigPath" -ConfigPrefix "k8s" -ResourcePath "$ResourcePath" -ResourcePrefix "k8s"
###################################################################

###################################################################
# Delete the container registry (not required for k8s)
. "$($rootPath)/container-registry/delete.ps1" -ConfigPath "$ConfigPath" -ConfigPrefix "cr" -ResourcePath "$ResourcePath" -ResourcePrefix "cr"
###################################################################
