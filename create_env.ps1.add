###################################################################
# Create the k8s cluster
. "$($rootPath)/kubernetes/create.ps1" -ConfigPath "$ConfigPath" -ConfigPrefix "k8s" -ResourcePath "$ResourcePath" -ResourcePrefix "k8s"
###################################################################

###################################################################
# Create the container registry (not required for k8s)
. "$($rootPath)/container-registry/create.ps1" -ConfigPath "$ConfigPath" -ConfigPrefix "cr" -ResourcePath "$ResourcePath" -ResourcePrefix "cr"
###################################################################
